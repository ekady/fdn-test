{
  "name": "fdn-technical-assessment",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "apps",
    "lib-components"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "apps:build": "npm run build -w apps",
    "apps:dev": "npm run dev -w apps",
    "lib:build": "npm run build -w lib-components",
    "lib:dev": "npm run dev -w lib-components"
  },
  "engines": {
    "node": ">=18.12.0",
    "yarn": ">=1.22.0",
    "npm": "please-use-yarn"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "turbo": "^1.10.16"
  }
}
