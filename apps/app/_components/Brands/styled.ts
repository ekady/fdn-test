import styled from 'styled-components';

export const BrandsImageWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;

  img {
    height: 80px;
    width: auto;
    object-fit: contain;
  }
`;
